post:
  summary: Classify the given unit
  description: Classify whether a unit is Weight, Distance or Volume
  tags:
    - Units
  requestBody:
    description: The unit to classify
    required: true
    content:
      application/text:
        schema:
          type: string
          example: km
  responses:
    "200":
      description: The unit classification
      content:
        application/json:
          schema:
            type: string
            enum: [DISTANCE, VOLUME, WEIGHT]
            example: DISTANCE