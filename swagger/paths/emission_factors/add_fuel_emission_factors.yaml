post:
  summary: Add multiple fuel emission factors
  description: Function that adds multiple fuel emission factors to the Database
  tags:
    - Emission factors
  parameters:
    - in : body
      name: code
      schema:
        type: array
        items:
          $ref: "#/components/schemas/FuelEmissionFactor"
      description: The fuel emission factors to add
  responses:
    "200":
      description: List of added fuel emission factors
      content:
        application/json:
          schema:
            type: array
            items:
              $ref: "#/components/schemas/FuelEmissionFactor"