name: Pandoc

on:
  push:
    paths:
      - Documentation/ProjectPlan/**
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
          
      - uses: docker://pandoc/core:2.9
        with:
          args: "./Documentation/ProjectPlan/ProjectPlan.md > ProjectPlan.pdf" # gets appended to pandoc command
